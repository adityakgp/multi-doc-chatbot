run on terminal --> pip install -r requirements.txt to install all the required dependencies
replace the replicate api key with your api key in the env file
run on terminal --> streamlit run script.py 
